[
  {
    "id": "table-driven",
    "prompt": "Write tests for the Clamp function in clamp.go following our Go testing conventions.",
    "allowed_tools": "Read Write Edit Glob Grep Skill Bash(go *) Bash(gofmt *) Bash(mkdir *)",
    "files": {
      "go.mod": "module example.com/testdemo\n\ngo 1.24\n",
      "clamp.go": "// Package clamp provides bounded integer helpers.\npackage clamp\n\n// Clamp limits v to the inclusive range [lo, hi].\nfunc Clamp(v, lo, hi int) int {\n\tif v < lo {\n\t\treturn lo\n\t}\n\tif v > hi {\n\t\treturn hi\n\t}\n\treturn v\n}\n"
    },
    "assertions": [
      { "type": "file_exists", "path": "clamp_test.go" },
      { "type": "regex", "path": "clamp_test.go", "pattern": "\\[\\]struct" },
      { "type": "regex", "path": "clamp_test.go", "pattern": "t\\.Run\\(" },
      { "type": "not_regex", "path": "clamp_test.go", "pattern": "testify|gomega" },
      { "type": "command", "run": "go test ./...", "requires": "go" }
    ]
  },
  {
    "id": "fuzz-target",
    "prompt": "Add a fuzz test with a seed corpus for the Parse function to parse_test.go, following our Go fuzzing conventions.",
    "allowed_tools": "Read Write Edit Glob Grep Skill Bash(go *) Bash(gofmt *) Bash(mkdir *)",
    "files": {
      "go.mod": "module example.com/fuzzdemo\n\ngo 1.24\n",
      "parse.go": "// Package keyval parses key=value pairs.\npackage keyval\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n)\n\n// Parse splits \"key=value\" into its parts. The key must be non-empty.\nfunc Parse(s string) (key, value string, err error) {\n\tkey, value, ok := strings.Cut(s, \"=\")\n\tif !ok {\n\t\treturn \"\", \"\", fmt.Errorf(\"parse %q: missing '='\", s)\n\t}\n\tif key == \"\" {\n\t\treturn \"\", \"\", fmt.Errorf(\"parse %q: empty key\", s)\n\t}\n\treturn key, value, nil\n}\n"
    },
    "assertions": [
      { "type": "regex", "path": "parse_test.go", "pattern": "func Fuzz" },
      { "type": "regex", "path": "parse_test.go", "pattern": "f\\.Add\\(" },
      { "type": "regex", "path": "parse_test.go", "pattern": "f\\.Fuzz\\(" },
      { "type": "not_regex", "path": "parse_test.go", "pattern": "testify|gomega" },
      { "type": "command", "run": "go test ./...", "requires": "go" }
    ]
  }
]
